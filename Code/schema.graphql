schema {
    query: Query
}

type Post {
    id: ID!
    title: String!
    description: String!
    Origin: String!
}

type Mutation{
    insert_todos{objects:[] ]}
}

type Query {
    listPosts: PostsResult!
    getPost(id: ID!): PostResult!
}